{% extends 'admin/admin.html.twig' %}

{% block title %}
    {% if thank.title %}
        Modifier le remerciement {{ thank.title }}
    {% else %}
        Créer un remerciement
    {% endif %}
{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_rest(form) }}
    <div class="form-group">
        <button class="btn btn-primary">
            <i class="fa fa-save"></i> Enregistrer
        </button>
    </div>
    {{ form_start(form) }}
{% endblock %}